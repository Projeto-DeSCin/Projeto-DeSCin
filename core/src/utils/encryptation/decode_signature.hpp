#ifndef DECODE_SIGNATURE_HPP
#define DECODE_SIGNATURE_HPP

#include <vector>
#include <string>

std::vector<unsigned char> decode_signature(const std::string& signature);

#endif // DECODE_SIGNATURE_HPP
